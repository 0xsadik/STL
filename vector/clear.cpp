#include<bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> v;

    v.push_back(2);
    v.push_back(3);
    v.push_back(4);
    v.push_back(5);

    // to clear entire vector 
    v.clear();
    // let's be sure what is the size 
    cout << v.size() << '\n';

    return 0;
}